# en.dev Homebrew Tap

Homebrew formulae for en.dev tools.

```sh
brew install endevco/tap/aube
```

This tap carries aube while it is in beta. Once aube is stable enough
for Homebrew/core, the main formula can move there.
